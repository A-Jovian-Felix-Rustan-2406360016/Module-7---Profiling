version: '3.8'
services:
  db:
    image: postgres:latest
    container_name: pg-profiling
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=my-password
      - POSTGRES_DB=advpro-2024
    ports:
      - '5432:5432'
    restart: always